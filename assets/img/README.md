Put your photo here as `daniel-photo.jpg` (portrait orientation works best,
roughly 4:5 — e.g. 960x1200px). The homepage expects exactly this filename:

    assets/img/daniel-photo.jpg

If you'd rather use a different name or format, update the `src` in
`_layouts/home.html`.
